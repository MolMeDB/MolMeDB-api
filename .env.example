APP_NAME=MolMeDB
APP_ENV=local
APP_KEY= 
APP_DEBUG=true 
APP_TIMEZONE=Europe/Prague

APP_URL=http://localhost:9080
ASSET_URL="${APP_URL}"

CDK_DEPICT_URL=cdkdepict:8080
RDKIT_HOST=rdkit

###########################
#### FRONTEND SETTINGS ####
###########################
FRONTEND_URL=http://frontend:3000 # Change to real domain
NODE_ENV=develop
NEXT_PUBLIC_BACKEND_URL="${APP_URL}"
JWT_SECRET="${APP_KEY}"

COOKIES_BACKEND_XSRF_KEY=XSRF-TOKEN
COOKIES_BACKEND_SESSION_KEY=pokusnice_session
COOKIES_BACKEND_SESSION_USER_KEY=pokusnice_user
###########################

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=pgsql
DB_HOST=db
DB_PORT=5432
DB_DATABASE=molmedb
DB_USERNAME=postgres
DB_PASSWORD=admin123456

RDKIT_HOST='http://localhost:9696'

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@molmedb.upol.cz"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"
